#include "slam/orbslam3_adapter.hpp"

#include <System.h>

// Compile the upstream headers here without exposing them through the public API.
// Method definitions are deferred until initialization and shutdown are settled.
